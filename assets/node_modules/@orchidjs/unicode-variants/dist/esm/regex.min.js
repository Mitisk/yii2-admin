export const arrayToPattern=chars=>{chars=chars.filter(Boolean);if(chars.length<2){return chars[0]||""}return maxValueLength(chars)==1?"["+chars.join("")+"]":"(?:"+chars.join("|")+")"};export const sequencePattern=array=>{if(!hasDuplicates(array)){return array.join("")}let pattern="";let prev_char_count=0;const prev_pattern=()=>{if(prev_char_count>1){pattern+="{"+prev_char_count+"}"}};array.forEach((char,i)=>{if(char===array[i-1]){prev_char_count++;return}prev_pattern();pattern+=char;prev_char_count=1});prev_pattern();return pattern};export const setToPattern=chars=>{let array=Array.from(chars);return arrayToPattern(array)};export const hasDuplicates=array=>{return new Set(array).size!==array.length};export const escape_regex=str=>{return(str+"").replace(/([\$\(\)\*\+\.\?\[\]\^\{\|\}\\])/gu,"\\$1")};export const maxValueLength=array=>{return array.reduce((longest,value)=>Math.max(longest,unicodeLength(value)),0)};export const unicodeLength=str=>{return Array.from(str).length};